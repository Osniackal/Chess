package sample.util;

import javafx.scene.paint.Color;

public interface Settings {
    int GAME_VIEW_WIDTH = 800, GAME_VIEW_HEIGHT = 600;
    int BOARD_SIZE = 8;
    Color WHITE = Color.rgb(200, 200, 200), BLACK = Color.rgb(30, 30, 30);
}
